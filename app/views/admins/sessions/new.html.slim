.auth-container
  .auth-card-custom
    h2.auth-title ログイン
    p.auth-subtitle 管理者としてログイン
    = form_for(resource, as: resource_name, url: session_path(resource_name)) do |f|
      .form-group-custom
        = f.label :email, 'メールアドレス'
        = f.email_field :email, autofocus: true, autocomplete: "email", placeholder: "admin@mail.com"

      .form-group-custom
        = f.label :password, 'パスワード'
        = f.password_field :password, autocomplete: "current-password"

      - if devise_mapping.rememberable?
        .form-check-custom
          = f.check_box :remember_me
          = f.label :remember_me, 'ログインを記憶'

      .form-group-custom
        = f.submit "ログイン", class: "btn-primary-custom w-100"

    .auth-links
      = render "admins/shared/links"
